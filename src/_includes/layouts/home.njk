{% extends 'layouts/base.njk' %}
{% set pageType = 'home' %}

{% block content %}
  <main id="home-main">
    <section id="home-content">
      {{ content | safe }}
      {#
      <ul id="recent-posts" class="posts">
        {% for post in collections.recent_posts %}
          <li>
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <small>{{ post.date | dateFilter }}</small>
          </li>
        {% endfor %}
      </ul>
      <a href="/posts" id="view-all">View all posts</a>
      #}
    </section>
    <header id="home-header">
      <img src="/images/matthew-lettini-snow.jpg" alt="{{ site.authorName }}">
    </header>
  </main>
{% endblock %}
